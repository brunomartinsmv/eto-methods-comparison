# Legacy notebooks

These notebooks are retained for historical context only.

- `calculo_cuiaba.ipynb` contains old exploratory code, including broken raw-data paths such as `data/raw/data/raw/Evapo.xlsx` and duplicated metric/plotting logic.
- `calculo_piracicaba.ipynb` contains exploratory calculations and manuscript-era outputs that predate the current CLI pipeline.

Use `notebooks/eto_results_overview.ipynb` as the executable narrative notebook. Core cleaning, aggregation, metrics, plotting, and data-quality logic should live in `scripts/`, not in these legacy notebooks.
